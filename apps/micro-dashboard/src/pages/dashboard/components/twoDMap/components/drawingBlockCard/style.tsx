import styled from '@emotion/styled'
import { animated } from '@react-spring/web'

export const DrawingBlockCardWrapper = styled(animated.div)`
  position: fixed;
  z-index: 10;
  top: 24px;
  right: 24px;
  width: 300px;
`
